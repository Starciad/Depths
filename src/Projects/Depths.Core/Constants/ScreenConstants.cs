namespace Depths.Core.Constants
{
    internal static class ScreenConstants
    {
        internal const int SCREEN_WIDTH = GAME_WIDTH * SCALE_FACTOR;
        internal const int SCREEN_HEIGHT = GAME_HEIGHT * SCALE_FACTOR;

        internal const byte GAME_WIDTH = 84;
        internal const byte GAME_HEIGHT = 48;

        internal const byte SCALE_FACTOR = 12;
    }
}
